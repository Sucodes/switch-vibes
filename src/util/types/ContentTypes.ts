export type CardDataProps = {
  image: any;
  title: string;
  subTitle: string;
  links: string[];
};

export type PerkDataProps = {
  heading: string;
  subheading: string;
};

export type OtherProps = {
  title: string;
  answer?: string;
  text?: string;
};
